# Used only for local `npm run dev` / `npm run build` outside Docker.
# Inside Docker, the API URL is set via the API_URL container env var
# instead (see frontend/docker-entrypoint.sh and the root docker-compose.yml).
VITE_API_URL=http://localhost:8000
